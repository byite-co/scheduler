# MILESTONES.md — 쌤플래너 빌드 순서 + 수용 기준

> 이 순서대로 진행한다. **한 마일스톤 = 구현 → 테스트 작성 → lint/typecheck/test 통과 → PR 1개 → 멈추고 리뷰.**
> 수용 기준(✅)을 모두 만족해야 다음으로 넘어간다. 모호하면 추측하지 말고 질문한다(AGENTS.md §6).
> 외부 비용/부작용(결제·푸시·AI)은 먼저 mock/스텁, 실연동은 키가 준비된 뒤.

---

## M0 — 스캐폴딩 & 기반
모노레포(pnpm+Turborepo), `apps/student`(Expo+TS), `apps/teacher`(Next.js+TS),
`packages/shared`(타입·supabase 클라이언트), `packages/design-tokens`(§디자인 토큰),
`supabase/`(schema.sql 적용), CI(lint·typecheck·test·build), `.env.example`.
- ✅ 두 앱이 빈 화면으로 부팅된다(에뮬레이터/브라우저).
- ✅ `pnpm -w lint && pnpm -w typecheck && pnpm -w test`가 green, CI 통과.
- ✅ `supabase start` + `supabase db reset`로 schema.sql 적용 성공, 타입 생성(`packages/shared`).
- ✅ 디자인 토큰이 양 앱에서 import되어 색/폰트가 적용된다.

## M1 — 인증 · 온보딩 · 연결(핸드셰이크)
가입/로그인(소셜+이메일, 이메일 인증), 비밀번호 찾기/재설정, 역할,
학생 프로필 / 과외쌤 프로필·온보딩(A2~A4), 초대코드 발급, 연결 핸드셰이크(pending/active/rejected),
공개 범위(disclosure) 설정, 과외쌤 학생별 설정(검사 과목·리포트 주기), 보호자 동의(만14세 미만).
- ✅ 과외쌤이 초대코드 생성 → 학생이 코드로 연결 요청 → 과외쌤 수락 → 양쪽 상태가 active로 반영.
- ✅ 거절/대기 상태가 각각 올바른 화면으로 표시(한 화면에 몰아넣지 않음).
- ✅ 학생이 disclosure를 끄면 과외쌤 쪽에서 해당 데이터/사진이 안 보인다(RLS/뷰로 검증하는 테스트).
- ✅ 비밀번호 재설정·이메일 인증 동작. 만14세 미만 가입 시 보호자 동의 단계 강제.
- ✅ 화면이 `docs/ui-catalog/` 해당 PNG와 일치.

## M2 — 홈(오늘) · 플래너 · 할 일
학생 홈(과외생/혼공/제로 상태), 플래너(할일/시간표 일·주/캘린더, 혼공/제로),
할 일 CRUD + AI 검사 토글(본인) + 선생님 숙제(잠금), 시간표 블록 CRUD, streak 계산.
- ✅ 할 일 생성/완료가 Supabase와 동기화. 선생님 숙제는 학생이 AI검사 토글 **불가**(locked).
- ✅ 혼공생 홈은 "선생님 숙제" 섹션 숨김 + 또래 랭킹 표시, 과외생은 반대.
- ✅ 제로 상태(데이터 없음)에서 동기부여형 빈 화면 + "첫 할 일 추가" CTA.
- ✅ streak가 정확히 계산되고 하루 누락 시 비난 없는 톤.

## M3 — 타이머 · 집중 모드(온디바이스)
공부 타이머(과목 선택·세션 저장), 집중 모드(카메라 권한 G4/G5, **온디바이스 졸음 감지(포그라운드, 무업로드)**,
졸음 환기, 세션 요약, 집중 리포트), 집중 설정.
- ✅ 타이머 세션이 `study_sessions`에 저장, 누적/주간 추이 집계.
- ✅ 집중 모드가 기기 내 카메라로 동작, **이미지/영상이 Storage·서버로 절대 안 올라간다(네트워크/스토리지 쓰기 없음 테스트)**.
- ✅ UI에 "영상은 기기를 떠나지 않아요" 노출. 졸음 횟수/집중률은 메타데이터로만 저장.
- ✅ 권한 거부 시 타이머만 계속하는 폴백 동선.

## M4 — AI 완료검사(플래그십)
숙제 제출(C4, 사진 업로드) → `ai-homework-check`(통과/미흡/애매+확신도+사유) → 학생 결과
(과외생=선생님 코멘트 포함 / **혼공생=AI 단독**) → 과외쌤 검사 화면(통과/애매/미흡, 확인/반려/다시 제출).
- ✅ 제출→AI 판정→(과외생)선생님 확인→결과 루프 완성. 혼공생은 선생님 단계 없이 AI 결과.
- ✅ AI 검사 여부 결정: 본인 할 일=학생 / 선생님 숙제=과외쌤(출제 때). 채점이 아니라 완료 확인.
- ✅ 반려 시 학생 홈/숙제에 "다시 제출 요청" 진입 → 재제출 동작.
- ✅ 업로드 실패(H2)·검사 실패(H3) 에러 상태 처리.
- ※ AI는 먼저 스텁(고정 응답)으로 플로우 완성 → 키 준비 후 실연동.

## M5 — AI 공부량 추천 · 리포트
AI 공부량 추천(C7, 플래너 반영) + `ai-study-rec`, 학생 나의 리포트(C8) + `ai-report-draft`,
과외쌤 수업/주간 리포트 빌더(차트 + AI 초안 + 담을 과목 + 한 줄 코멘트) → 학부모 발송(공유 링크)
→ 학부모 웹뷰(`report-share`) + 히스토리 + 링크 만료 + 발송 완료/실패.
- ✅ 추천 생성 후 "플래너에 반영" 동작. 리포트 집계 차트가 데이터와 일치.
- ✅ 학부모 공유 링크가 인증 없이 열리고 만료가 동작(`report_views` 기록).
- ✅ 무료=광고 보상 언락 / 프리미엄=무제한 게이팅이 나의 리포트·AI추천에 적용.

## M6 — 수익화(★ 별개 모듈 분리)
학생 프리미엄(IAP)+광고 보상 언락 / 과외쌤 **앱 구독료**(Stripe: 연결 학생 수×₩2,900, 인보이스,
결제 실패·미납 복구, 해지·일시정지) / **수업·수업료(수기 트래커, 결제 처리 아님)**.
- ✅ 과외쌤 월 청구액 = `active 연결 수 × 2900`. 학생 연결 해제 시 다음 청구에서 감소.
- ✅ Stripe/IAP 웹훅이 구독 상태 갱신. 미납(past_due) 시 기능 제한 → 결제수단 업데이트 → 복구.
- ✅ 게이팅(무료/언락/프리미엄) 정확. "앱 구독료"와 "수업료"가 코드/화면에서 명확히 분리.
- ✅ 가격은 상수/설정(하드코딩 산재 금지).

## M7 — 알림 · 계정 · 시스템 상태
푸시 권한 프라이밍/거부 + 토큰 등록 + 알림 센터(딥링크), 계정 설정·프로필 편집·약관 뷰어·
**회원 탈퇴(다단계, 영구 삭제 고지)**, 에러/로딩/빈 상태, 강제 업데이트, 점검 중, 첫 사용 코치마크.
- ✅ 푸시 권한 옵트인 → 토큰 저장 → 알림 수신·딥링크. 권한 거부 시 기능은 막지 않음.
- ✅ 회원 탈퇴 시 데이터 cascade 삭제(연결·구독 영향 안내). 본인 확인 단계.
- ✅ 강제 업데이트/점검 중/네트워크 오류 등 모든 시스템 상태가 도달 가능.

## M8 — 폴리시 & QA
접근성(AA 대비), 마이크로카피 전수 교정, 디자인 토큰 준수, 크로스 기기(데스크톱/태블릿/모바일),
E2E 테스트(과외쌤=Playwright, 학생=Maestro/Detox).
- ✅ 핵심 플로우 E2E green. 접근성 대비 검사 통과.
- ✅ 모든 화면이 `docs/ui-catalog/`와 일치(레이아웃·카피·상태).
- ✅ 카피 오타/존댓말 일관, 칩 nowrap, 색 토큰 외 임의색 없음.

---

### 마일스톤 공통 PR 체크리스트
- [ ] lint · typecheck · test green
- [ ] 새/변경 화면이 카탈로그 PNG와 일치
- [ ] 데이터 접근이 RLS/disclosure를 준수(테스트 포함)
- [ ] 비밀/대용량 미커밋, `.env.example` 갱신
- [ ] PR 본문에 변경 요약 + 충족한 수용 기준 + 사람이 확인할 점
